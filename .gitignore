# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# Sanity-sourced posts (regenerated on every build, never committed)
_posts/sanity/

# pnpm lockfile (matches existing package-lock.json exclusion)
pnpm-lock.yaml

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

# Wrangler
.wrangler

# Migration tool output
tools/migrate-mt-image-map.yml
